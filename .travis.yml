language: cpp

compiler:
  - gcc
  - clang

before_script:
  - ./configure --defines=JDKSAVDECCMCU_ARDUINO=1

script:
  - make && make check

notifications:
  recipients:
    - jeffk@jdkoftinoff.com
  email:
    on_success: change
    on_failure: always

os:
  - linux
  - osx

